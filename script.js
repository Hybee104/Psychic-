
            function scrolling(div) {
                $('html,body').animate(
                   {
                      scrollTop: $("#" + div).offset().top
                   }, 'slow');
             }

                  // alert(" Get a free reading xxx ")
